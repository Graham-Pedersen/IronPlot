This is the reopository for IronPlot a scheme-like language

How it works

.plot file -> (flex parser) -> parse tree -> (desugaring) -> core scheme parse tree -> (.IL code emmission) -> .NET intermediate language .exe file

input grammer:

core scheme grammer:



How to compile the compilers

Invoking the compiler:

Creating .dll librarys:
